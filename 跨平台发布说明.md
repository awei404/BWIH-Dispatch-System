# BWIH 调度系统：独立应用发布说明

## macOS 独立版

文件：`BWIH调度系统-macOS-AppleSilicon.zip`

适用：Apple Silicon Mac（M1、M2、M3、M4），macOS 12 或更新版本。

使用方法：解压后双击 `BWIH 调度系统（独立版）.app`。不需要安装 Python，也不需要打开终端。第一次使用时会在该用户电脑中自动建立一套新的空白数据库。

若 macOS 提示无法确认开发者：按住 Control 点击应用，再选择“打开”，然后再次确认即可。

## Windows 独立版

适用：Windows 10 / 11 64 位。

Windows 版本必须由 Windows 环境生成，因此不能由 macOS 直接产出。项目内已备好两种生成方式：

1. 在 Windows 电脑安装 Python 3.12 后，双击或运行 `packaging\\build_windows.bat`；输出在 `dist\\BWIH Dispatch`，其中 `BWIH Dispatch.exe` 可直接运行。
2. 将项目放入 GitHub 仓库后，在 Actions 中运行 `Build Windows desktop app` 工作流；它会自动生成可下载的 Windows 独立应用。

Windows 用户运行时不需要安装 Python。

## 数据与隐私

- 每位使用者的司机、Check-in、评分、照片与上传表格都只保存在其自己的电脑。
- macOS 数据位置：`~/Library/Application Support/BWIH Dispatch`
- Windows 数据位置：`%LOCALAPPDATA%\\BWIH Dispatch`
- 发送应用包不会携带当前这台电脑的 `dispatch.db` 或 `uploads` 内容。
